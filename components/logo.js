import React from 'react';
import logo from '../img/logo.png';

function Logo() {
	console.log(house);
	return (
		<div className="Logo">
			<img src={logo} alt="logo"/>
		</div>
	);
}

export default Logo;