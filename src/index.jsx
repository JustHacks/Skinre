import React from 'react';
import ReactDOM from 'react-dom';
import './style.css';

const App = (props) => {
    return (
        <main>
            <h1>tailwind</h1>
        </main>
    );
};

ReactDOM.render(
    <App />,
    document.getElementById('root')
);